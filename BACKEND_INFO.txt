Hey Antigravity, tengo 3 bugs del backend que necesito que arregles:

1. GET /transactions devuelve 403 "solo administradores" para usuarios normales. Necesitan ver SU propio historial. Ábrelo para que cualquier usuario logueado pueda leer sus propias transacciones (filtrando por from/to = nick del JWT).

2. GET /users/sofii devuelve 500. Otros usuarios sí funcionan. Revisa si hay algún dato corrupto en ese doc.

3. GET /profiles/:nick/comments devuelve 404. El endpoint no existe. Necesito que crees GET /profiles/:nick/comments que devuelva los comentarios de perfil de ese usuario.

Gracias 🙏
