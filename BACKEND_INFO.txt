Antigravity, necesito 2 fixes urgentes en el backend:

1. El endpoint POST /api/auth/login debe incluir el campo "hash" del usuario en la respuesta JSON. Actualmente solo devuelve { message, nick, isAdmin, accessToken, refreshToken }. Necesito que devuelva también el hash del usuario para que el auto-login del frontend funcione. Ejemplo:
{ "message": "...", "nick": "solariswat", "isAdmin": true, "hash": "s2_xxx", "accessToken": "...", "refreshToken": "..." }

2. Falta el endpoint GET /api/bank_accounts/:nick — necesito que devuelva una sola cuenta bancaria por nick. Ya existe el GET /api/bank_accounts (listado) y PUT /api/bank_accounts/:nick (update), pero falta el GET individual. Si el nick no existe, devolver 404 con { error: "Cuenta no encontrada" }.
