Estoy configurando Input Leap como CLIENTE en mi máquina CachyOS (Arch Linux) usando Hyprland (Wayland).
El servidor es una máquina Windows en la IP 192.168.0.20 (puerto 24800, sin SSL / --disable-crypto).

Al darle a Start en Input Leap, me sale este error en el log:
"error to initialize remote desktop session: GDBus.error:org.freedesktop.DBus.Error.InvalidArgs: la interfaz 'org.freedesktop.portal.RemoteDesktop' no existe"

Por favor, ayúdame a resolver esto en mi sistema para que funcione como cliente de Input Leap:
1. Revisa e instala los paquetes necesarios (libei, xdg-desktop-portal-hyprland, etc.) o configúralos adecuadamente.
2. Si la interfaz RemoteDesktop de xdg-desktop-portal da problemas con Hyprland, proporciona el comando o script para arrancar el cliente de Input Leap usando XWayland / X11 fallback o directamente via CLI (ej. `input-leapc -f --name cachyos-x8664 --disable-crypto 192.168.0.20`).
3. Ejecuta los comandos necesarios para dejarlo conectado y funcionando.
