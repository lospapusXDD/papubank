ANTIGRAVITY — setup sigue en 500 en el túnel NUEVO, tu prueba fue incompleta

Probé contra https://pcs-willow-investigation-milton.trycloudflare.com/api con token autenticado real (usuario recién creado test_2fa_probe4, cuenta fresca sin 2FA):

POST /auth/2fa/setup con body {}, {"nick":"..."} y {"label":"..."} → 500 "Error interno del servidor" en los tres casos
Tu prueba (401 "Token requerido") solo cubre la ruta SIN autenticar — no toca el código de otplib
Mi hipótesis basada en la evidencia: el 500 NO está en la generación del secret, está en la ESCRITURA a DB. Fíjate en el patrón:

setup → genera secret, luego ESCRIBE twofa_pending_secret → 500
verify → valida, luego ESCRIBE twofa_secret/twofa_enabled/twofa_backup_codes → 500
disable / backup-codes → validan estado y responden 400 ANTES de escribir → funcionan
confirm → valida tempToken sin escribir → funciona
Es decir: las 4 rutas que tocan DB escriben con SQL directo y fallan. Probablemente los handlers usan UPDATE users SET ... con nombres de columna que no coinciden, o el fieldMap del bridge sigue incompleto para las escrituras internas.

Por favor:

En auth.js, cambia temporalmente el catch de setup a res.status(500).json({ error: String(err) }) y dime el mensaje SQL exacto que devuelve
Confirma que el proceso del túnel pcs-willow-investigation-milton arrancó DESDE el directorio con bridge.js y auth.js actualizados (git status + git log -1 en el servidor)
Pruébalo tú mismo autenticado: POST /api/auth/2fa/setup con Authorization: Bearer <token real> — no basta el 401 sin token
