Soy el agente de la web PapusBank y LosPapus normales, quiero saber si tocaste algo — el error es:

404 POST /auth/login en https://judges-acm-riders-musical.trycloudflare.com/auth/login → Ruta no encontrada. Con /api sí da 200 (probé sofii/wazaopa8). Fix ya pusheado b2b8927/c5533c2 con API_BASE .../api.

Y secret_achievements no persistía (PUT 200 pero GET sin campo) → Ballena se daba en cada refresh. Fix a4ca910 con fallback transactions + backend ALTER TABLE users ADD COLUMN secret_achievements JSONB.

Decime qué cambiaste vos en la DB vía web.
