El endpoint POST /api/bank/mint no funciona. Siempre responde {"error":"Destino y monto requeridos"} sin importar el body. 

Prueba que hice:
POST /api/bank/mint
Content-Type: application/json
Authorization: Bearer [token válido]
Body: {"destino":"solariswat","monto":500}

Respuesta: {"error":"Destino y monto requeridos"}

Lo mismo pasa con nick/amount. Parece que el body parser no está leyendo el JSON. Revisa que el endpoint esté leyendo req.body correctamente.

Mismo problema con POST /api/bank/burn.
