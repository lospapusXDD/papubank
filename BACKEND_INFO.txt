"Agrega los siguientes 2 endpoints en el backend de Node.js para soportar eliminación y edición de mensajes en PapuWhats:

DELETE /api/messages/:id -> Elimina el mensaje con el ID correspondiente de la base de datos.
PUT /api/messages/:id -> Recibe { msg } y actualiza el texto del mensaje con ese ID
