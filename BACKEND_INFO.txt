PUT /api/bank_accounts/:nick con el campo badges en el body devuelve 500 (columna inexistente). Esto rompe el Dedo de Sukuna (market.js) y el Omnitrix (ben10.js): la compra falla entera y el jugador pierde la plata sin recibir nada.

Por favor:

Agrega a bank_accounts una columna badges tipo JSONB (array de strings, default []).
Si puedes, haz el PUT tolerante: ignorar columnas desconocidas en vez de fallar (previene futuros 500 con campos nuevos).
Dime el esquema actual completo de bank_accounts (columnas y tipos) para que el frontend sepa qué campos puede escribir.
Borra la cuenta de prueba test_finger_x.
Dato extra: la cuenta test_finger_x se creó sola con 5000 PPC al hacer GET — asumo que hay lazy-create; si no es intencional, puede ser otro bug menor, revisa de paso.
