Hola. Estoy integrando un bot/automatizador de WhatsApp para mis proyectos utilizando Node.js (con Baileys/conexión WhatsApp) y necesito conectarlo a nuestra base de datos PostgreSQL.

Por favor, proporcióname un resumen técnico con lo siguiente:

1. **Esquema de Base de Datos actual:**
   - ¿Cuáles son las tablas existentes y sus columnas principales?
   - ¿Existe ya alguna tabla de contactos, usuarios, mensajes, tareas o estados de chat?

2. **Propuesta de tablas para el Bot de WhatsApp:**
   - Si no existen tablas para WhatsApp, ¿cómo recomiendas estructurar las tablas para:
     a) `whatsapp_contacts` (teléfono, nombre, fecha último contacto, etc.)
     b) `chat_states` o sesiones (para recordar el estado del usuario en el flujo de conversación)
     c) `chat_logs` o `tasks` (registro de interacciones o solicitudes)
   - Por favor, genera el script SQL (`CREATE TABLE ...`) adecuado para agregarlas sin alterar las tablas de mis otros proyectos.

3. **Formato de la Cadena de Conexión:**
   - Confírmame qué variables de entorno (`DATABASE_URL`, `PGUSER`, `PGHOST`, etc.) o credenciales mínimas necesitará el bot para conectarse vía `pg` (node-postgres) o SSL si aplica.

4. **Reglas de Integración / Triggers:**
   - ¿Hay alguna tabla que el bot deba consultar (lectura) o actualizar (escritura) de los proyectos existentes?
