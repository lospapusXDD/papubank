"La conexión del túnel funciona excelente, pero al hacer POST /auth/login con { nick: 'solariswat', password: '...' } el backend devuelve 500 Error interno del servidor.

Ocurre porque la función hashPass en el backend espera comprobar la clave con hashSalt="emilio".

Por favor revisa el bloque de POST /auth/login o POST /papuwhats/login en el servidor para envolver la autenticación en un try / catch limpio y devolver 401 Credenciales incorrectas en lugar de crash 500 si la clave o hash no coinciden."
